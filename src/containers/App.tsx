import * as React from 'react'

interface Props {
  // name?: string;
}
// const App: React.SFC<Props> = ({name}) => {
const App: React.SFC<Props> = () => {
  return (
    <div>
      App
    </div>
  )
}

export default App
