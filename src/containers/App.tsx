import * as React from 'react'

interface Props {
  // name?: string;
}
// const App: React.SFC<Props> = ({name}) => {
const App: React.SFC<Props> = () => {
  return (
    <div>
      from App
    </div>
  )
}

export default App
